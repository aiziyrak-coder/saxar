# HTTP (80) — SSL yo'q paytda yoki certbot dan oldin. Boshqa saytlarga tegmaydi.
upstream saxar_fe_http {
    server 127.0.0.1:18180;
    keepalive 8;
}

upstream saxar_api_http {
    server 127.0.0.1:18181;
    keepalive 8;
}

server {
    listen 80;
    listen [::]:80;
    server_name saxar.uz www.saxar.uz;

    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    client_max_body_size 25m;

    location /api/ {
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_set_header Host api;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://saxar_api_http;
        proxy_connect_timeout 60s;
        proxy_send_timeout 120s;
        proxy_read_timeout 120s;
    }

    location /api-auth/ {
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_set_header Host api;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://saxar_api_http;
        proxy_connect_timeout 60s;
        proxy_send_timeout 120s;
        proxy_read_timeout 120s;
    }

    location / {
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://saxar_fe_http;
        proxy_read_timeout 120s;
    }
}
